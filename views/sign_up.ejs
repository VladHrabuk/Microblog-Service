<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> ExploreX | <%= locals.pageTitle %>
    </title>
    <link href="./css/output.css" rel="stylesheet">
</head>

<body>
    <main>
        <section class="text-gray-600 body-font">

            <div class="container px-5 py-24 mx-auto">
                <div class="text-center mb-20">
                    <h1 class="title-font font-medium text-3xl text-gray-900">ExploreX Blog</h1>
                </div>
                <form action="/sign-up" method="POST"
                    class="md:w-1/3 bg-gray-100 rounded-lg p-8 flex flex-col mx-auto w-full mt-10 md:mt-0">
                    <h2 class="text-gray-900 text-lg font-medium title-font mb-5">Sign Up</h2>
                    <div class="relative mb-4">
                        <label for="username" class="leading-7 text-sm text-gray-600">Username</label>
                        <input type="text" id="username" name="username"
                            value="<%= locals.formData && locals.formData.username || '' %>"
                            class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        <% if (typeof errors !=='undefined' && errors.username ) { %>
                            <span class="text-red-400">
                                <%= errors.username.msg %>
                            </span>
                            <% } %>
                    </div>
                    <div class="relative mb-4">
                        <label for="name" class="leading-7 text-sm text-gray-600">Name</label>
                        <input type="text" id="name" name="name"
                            value="<%= locals.formData && locals.formData.name || '' %>"
                            class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        <% if (typeof errors !=='undefined' && errors.name ) { %>
                            <span class="text-red-400">
                                <%= errors.name.msg %>
                            </span>
                            <% } %>
                    </div>
                    <div class="relative mb-4">
                        <label for="password" class="leading-7 text-sm text-gray-600">Password</label>
                        <input type="password" id="password" name="password"
                            value="<%= locals.formData && locals.formData.password || '' %>"
                            class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        <% if (typeof errors !=='undefined' && errors.password ) { %>
                            <span class="text-red-400">
                                <%= errors.password.msg %>
                            </span>
                            <% } %>
                    </div>
                    <div class="relative mb-4">
                        <label for="password_repeat" class="leading-7 text-sm text-gray-600">Confirm Password</label>
                        <input type="password" id="password_repeat" name="password_repeat"
                            class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        <% if (typeof errors !=='undefined' && errors.password_repeat ) { %>
                            <span class="text-red-400">
                                <%= errors.password_repeat.msg %>
                            </span>
                            <% } %>
                    </div>
                    <button type="submit"
                        class="text-white bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg">Create
                        an account</button>
                    <p class="text-sm text-gray-500 mt-5">Already have an account? <a
                            class="text-blue-500 font-bold text-sm" href="/sign-in">Sign
                            in here</a></p>
                </form>
            </div>
        </section>
    </main>
</body>

</html>